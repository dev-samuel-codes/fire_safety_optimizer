# FireOpt 백엔드 API — 런타임 의존성
# 설치:  pip install -r requirements.txt

# --- API 서버 기동·DXF 처리·방추출·실 판정 ---
flask>=3.0
ezdxf>=1.3
numpy>=1.26       # raster 방추출
scipy>=1.11       # raster 방추출(morphology closing)
Pillow>=10.0      # raster 방추출(벽 래스터화)
shapely>=2.0      # 규칙엔진 기하 판정(면적·거리·피복)
networkx>=3.0     # 규칙엔진(경계구역/그래프)

# --- FireOpt 최적화/IFC/렌더 전용 (웹앱 미사용, 설치 까다로움 — 필요 시만) ---
# ifcopenshell, matplotlib

# 개발/테스트: pytest

# DWG 지원: 시스템에 dwg2dxf(libredwg)가 있어야 함(PATH 또는 DWG2DXF 환경변수).
#          없으면 DWG 업로드는 안내 메시지로 실패하고, DXF는 정상 동작.
